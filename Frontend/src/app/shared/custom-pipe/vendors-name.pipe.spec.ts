import { VendorsNamePipe } from './vendors-name.pipe';

describe('VendorsNamePipe', () => {
  it('create an instance', () => {
    const pipe = new VendorsNamePipe();
    expect(pipe).toBeTruthy();
  });
});
